const names = ["gana", "isms", "hhfuih"];

const q1 = names.map(function(name) {
    return name.toUpperCase();
});

console.log(q1);

//q2
const product=[
    {id:1,name:"Laptop"},
    {id:2,name:"Table"},
    {id:3,name:"Phone"}
];
 const p1 = product.map(function(Product) {
    return product.name;
});